<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Agent Request Update</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        padding: 2rem;
        background-color: #f9f9f9;
        color: #333;
      }
      .container {
        background-color: #fff;
        border-radius: 8px;
        padding: 2rem;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        max-width: 600px;
        margin: auto;
      }
      h1 {
        color: #2c3e50;
      }
      .status {
        font-weight: bold;
        color: #2980b9;
      }
      .footer {
        margin-top: 2rem;
        font-size: 0.9rem;
        color: #777;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Hello <%= name %>,</h1>
      <p>
        Your request to become an agent in <strong>Neela Wallet</strong> has
        been <span class="status"><%= status %></span>.
      </p>

      <% if (status === 'APPROVED') { %>
      <p>Welcome aboard! You now have access to agent features and tools. 🎉</p>
      <% } else if (status === 'REJECTED') { %>
      <p>
        Unfortunately, your request was rejected. Please contact support for
        further information.
      </p>
      <% } else if (status === 'SUSPENDED') { %>
      <p>
        Your agent status has been temporarily suspended. Kindly check with the
        admin for more details.
      </p>
      <% } else { %>
      <p>Please check your dashboard for the updated status.</p>
      <% } %>

      <p>
        If you have any questions or concerns, feel free to reply to this email
        or reach out to our support team.
      </p>

      <div class="footer">
        <p>Neela Wallet API Team</p>
        <p>
          &copy; <%= new Date().getFullYear() %> Neela Wallet. All rights
          reserved.
        </p>
      </div>
    </div>
  </body>
</html>
